<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            <server>, not <env> — see phpunit.xml's comment: this suite runs inside a
            docker-compose container that already exports these into $_SERVER, which Dotenv
            reads ahead of anything <env> can set.

            DB_* is deliberately NOT forced here (unlike phpunit.xml's sqlite override): this
            suite needs a *real* Postgres, and the docker-compose `api` service's own
            environment already points DB_HOST/DB_PORT/DB_DATABASE/DB_USERNAME/DB_PASSWORD at a
            working one — whatever POSTGRES_PASSWORD etc. a given machine's root .env sets.
            Hardcoding "qor"/"qor" here previously only *looked* correct because the old
            non-forcing <env> was a silent no-op that happened to fall through to the real
            (working) credentials; forcing wrong hardcoded ones would break real Postgres auth
            on any machine whose root .env deviates from docker-compose's bare defaults.
        -->
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <server name="FILESYSTEM_DISK" value="local"/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
    </php>
</phpunit>
